#include "net.h"

namespace net::arp
{
/*
    void net::arp::Base::_fill_header_eth(const eth::address::Custom & destination)
    {
        auto header_eth = eth::Header(_output);

        header_eth.destination = destination;
        header_eth.source = _interface_io.address();
        header_eth.type = eth::type::Arp;
    }
    void net::arp::Base(const eth::address::Custom & eth_destination, const ipv4::address::Custom & ipv4_destination)
    {
        auto header_arp = arp::Header(_output);

    }*/
}
